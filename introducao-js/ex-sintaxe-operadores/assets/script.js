function verificarNumeros(a, b) {
    var soma = a + b;
    
    
    if(a === b) {
        return `Os números ${a, b} são iguais. Sua soma e ${soma}, ${verficaMaiorDez}, ${verficaMaiorVinte}`
    } else {
        return `Os números ${a} e ${b} não são iguais. Sua soma e ${soma}, ${verficaMaiorDez}, ${verficaMaiorVinte}.`
    }
}

console.log(verificarNumeros(20 , 20))